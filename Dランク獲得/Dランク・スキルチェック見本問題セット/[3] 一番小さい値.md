# 一番小さい値

https://paiza.jp/works/mondai/d_rank_skillcheck_sample/min_num

<details><summary>Python3 解答</summary><br>

```python
n = [int(input()) for _ in range(5)]
print(min(n))
```

```python
n = []
for _ in range(5):
    n.append(int(input()))

print(min(n))
```

別解1-1<br>
条件に $`1 ≦ n_1, n_2, n_3, n_4, n_5 ≦ 100`$ とあるので、上限よりも大きい101で変数 `answer` を初期化し、その後入力から数字を受け取って小さい方に更新する、というのを5回繰り返せば良い。

```python
answer = 101
for _ in range(5):
    n = int(input())
    answer = min(answer, n)

print(answer)
```

別解1-2<br>
1個目の値を `answer` の初期値としても良い。ただし、1個目をすでに入力から読み取っている分、入力から受け取る回数が1回少なくなることに注意。<br>
また、入力をわざわざ `n` に格納しなくても良い。

```python
answer = int(input())
for _ in range(4):
    answer = min(answer, int(input()))

print(answer)
```

別解2<br>
`min` を使わずに、if文を使っても良い。

```python
answer = int(input())
for _ in range(4):
    n = int(input())
    if n < answer:
        answer = n

print(answer)
```

別解3-1<br>
リスト `n` をsortする。

```python
n = [int(input()) for _ in range(5)]
n.sort()
print(n[0])
```

別解3-2

```python
n = sorted(int(input()) for _ in range(5))
print(n[0])
```

<br></details>

<details><summary>C++ 解答</summary><br>

条件に $`1 ≦ n_1, n_2, n_3, n_4, n_5 ≦ 100`$ とあるので、上限よりも大きい101で変数 `answer` を初期化し、その後入力から数字を受け取って小さい方に更新する、というのを5回繰り返せば良い。

```cpp
#include <iostream>
using namespace std;

int main() {
    int answer = 101, n;
    for (int i = 0; i < 5; i++) {
        cin >> n;
        answer = min(answer, n);
    }
    cout << answer << endl;
}
```

少し変えた別解<br>
1個目の値を `answer` の初期値としても良い。ただし、1個目をすでに入力から読み取っている分、入力から受け取る回数が1回少なくなることに注意。

```cpp
#include <iostream>
using namespace std;

int main() {
    int answer, n;
    cin >> answer;
    for (int i = 0; i < 4; i++) {
        cin >> n;
        answer = min(answer, n);
    }
    cout << answer << endl;
}
```

別解2<br>
`min` を使わずに、if文を使っても良い。

```cpp
#include <iostream>
using namespace std;

int main() {
    int answer, n;
    cin >> answer;
    for (int i = 0; i < 4; i++) {
        cin >> n;
        if (n < answer) {
            answer = n;
        }
    }

    cout << answer << endl;
}
```

別解3

```cpp
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int n1, n2, n3, n4, n5;
    cin >> n1 >> n2 >> n3 >> n4 >> n5;

    cout << min({n1, n2, n3, n4, n5}) << endl;
}
```

別解4-1<br>
`vector` に対して `min_element` を使う。<br>
`vec.at(i)` は `vec[i]` でも可。

```cpp
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main() {
    vector<int> vec(5);
    for (int i = 0; i < 5; i++) {
        cin >> vec.at(i);
    }

    cout << *min_element(vec.begin(), vec.end()) << endl;
}
```

別解4-2<br>
Cの配列に対して `min_element` を使う。

```cpp
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int arr[5];
    for (int i = 0; i < 5; i++) {
        cin >> arr[i];
    }

    cout << *min_element(arr, arr + 5) << endl;
}
```

別解4-3<br>
`array` に対して `min_element` を使う。

```cpp
#include <iostream>
#include <algorithm>
#include <array>
using namespace std;

int main() {
    array<int, 5> arr;
    for (int i = 0; i < 5; i++) {
        cin >> arr[i];
    }

    cout << *min_element(arr.begin(), arr.end()) << endl;
}
```

別解5-1<br>
`vector` をsort。

```cpp
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main() {
    vector<int> vec(5);
    for (int i = 0; i < 5; i++) {
        cin >> vec.at(i);
    }

    sort(vec.begin(), vec.end());

    cout << vec.at(0) << endl;
}
```

別解5-2<br>
`vec.at(i)` は `vec[i]` でも可。

```cpp
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main() {
    vector<int> vec(5);
    for (int i = 0; i < 5; i++) {
        cin >> vec[i];
    }

    sort(vec.begin(), vec.end());

    cout << vec[0] << endl;
}
```

別解5-3<br>
Cの配列をsort。

```cpp
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int a[5];
    for (int i = 0; i < 5; i++) {
        cin >> a[i];
    }
    
    sort(a, a + 5);

    cout << a[0] << endl;
}
```

別解5-4<br>
`array` をsort。

```cpp
#include <iostream>
#include <algorithm>
#include <array>
using namespace std;

int main() {
    array<int, 5> arr;
    for (int i = 0; i < 5; i++) {
        cin >> arr[i];
    }

    sort(arr.begin(), arr.end());
    
    cout << arr[0] << endl;
}
```

<!--
別解3<br>
配列を使う

```cpp
#include <iostream>
using namespace std;

int main() {
    int a[5];
    for (int i = 0; i < 5; i++) {
        cin >> a[i];
    }

    int answer = a[0];
    for (int i = 1; i < 5; i++) {
        answer = min(answer, a[i]);
    }

    cout << answer << endl;
}
```

別解4<br>
`vector` を使う

```cpp
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> v(5);
    for (int i = 0; i < 5; i++) {
        cin >> v[i];
    }

    int answer = v[0];
    for (int i = 1; i < 5; i++) {
        answer = min(answer, v[i]);
    }

    cout << answer << endl;
}
```
-->

<br></details>
